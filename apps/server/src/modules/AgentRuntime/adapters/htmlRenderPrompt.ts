/**
 * Re-exported from @lobechat/const so both the server context builder and the
 * client-side direct-chat path append the same preset.
 */
export { buildSystemRole, HTML_RENDER_PROMPT } from '@lobechat/const';
